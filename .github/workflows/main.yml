on: [push]
name: build gatsby
jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: actions/checkout@develop
      - name: hello worl action
        uses: actions/hello-world-javascript-action@v1
        id: hello
        with:
            who-to-greet: 'kyle roux'
      - name: get outpout
        run: echo "the time was ${{steps.hello.outputs.time}}"

      - name: install
        run: yarn install

      - name: build
        run: yarn build

      - name: deploy
        run: yarn gh-pages
