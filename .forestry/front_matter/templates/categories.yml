---
label: Categories
hide_body: true
fields:
- name: categories
  type: field_group_list
  fields:
  - name: name
    type: text
    config:
      required: true
    label: Name
    description: category name
  config:
    min: 
    max: 
    labelField: name
  label: Categories
  description: post categories
pages:
- src/data/categories.yaml
